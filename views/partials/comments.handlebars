<div class="comments">
  {{#each this}}
    <section class="comment">
      <div class="text">
        {{comment_text}}
      </div>
      <div class="meta">
        {{User.username}}
        on
        {{format_date createdAt}}
      </div>
    </section>
  {{/each}}
</div>